[project]
name = "avps-drhfpnc"
version = "0.1.0"
description = "Conversion des AVPS de la DRHFPNC en Markdown et CSV"
requires-python = ">=3.10"
dependencies = [
    "marker-pdf>=1.10.2",
    "pandas",
    "pyarrow",
    "requests",
    "pytz",
]
