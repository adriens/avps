name: Daily Data Update

on:
  schedule:
    - cron: '0 0 * * *' # S'exécute tous les jours à minuit
  workflow_dispatch: # Permet de le lancer manuellement

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebp-dev libharfbuzz-dev libfribidi-dev libxcb1-dev

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install Python dependencies
        run: uv sync

      - name: Run processing script
        run: uv run src/process_avps.py

      - name: Build site with MkDocs
        run: uv run mkdocs build

      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add data/ mkdocs.yml
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            TAG_DATE=$(date +'%Y-%m-%d')
            git commit -m "chore: update AVPs data ($TAG_DATE)"
            
            # Récupérer les tags distants pour vérifier l'existence
            git fetch --tags
            
            TAG_NAME="$TAG_DATE"
            if git rev-parse "$TAG_NAME" >/dev/null 2>&1; then
              COUNT=1
              while git rev-parse "$TAG_NAME.$COUNT" >/dev/null 2>&1; do
                COUNT=$((COUNT + 1))
              done
              TAG_NAME="$TAG_NAME.$COUNT"
            fi
            
            echo "Creating tag: $TAG_NAME"
            git tag "$TAG_NAME"
            git push origin main --tags
            git push origin main
          fi

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./site
